.talkroom
  %p.room_title トークルーム
  - if @messages.present?
    - @messages.each do |message|
      - if message.user.id == current_user_id
        .my_talk
          .my_talk_img= image_tag message.user.avatar_image, class: 'msg_avatar'
          .my_talk_content= message.content
        .clear
      - else
        .talk
          .talk_img= image_tag message.user.avatar_image, class: 'msg_avatar'
          .talk_content= message.content
        .clear
  - else
    %p メッセージがありません

  = form_for @message do |f|
    .msg_form_container
      = f.text_field :content, :placeholder => 'メッセージを入力してください', size: 70, class: 'form msg_form'
      = f.hidden_field :room_id, value: @room.id
      %br/
      = f.submit '投稿する', class: 'btn submit_btn msg_submit' 