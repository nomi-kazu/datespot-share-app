.container 
  .content_right
    = render 'commons/content_right'

  .content_left 
    = "「 #{@search} 」の検索結果"
    - if @posts.present?
      - @posts.each do |post|
        = render 'commons/posts', post: post
      = paginate @posts
    - else
      %p.mt-3 投稿がありません
    